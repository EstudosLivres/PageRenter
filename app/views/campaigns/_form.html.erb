<%= form_for(@campaign) do |f| %>
    <% if @campaign.errors.any? %>
        <div id="error_explanation">
            <h2><%= pluralize(@campaign.errors.count, "error") %> prohibited this campaign from being saved:</h2>

            <ul>
                <% @campaign.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                <% end %>
            </ul>
        </div>
    <% end %>

    <%= f.file_field :advertising_piece %>

    <div class="field">
        <%= f.label :name %><br>
        <%= f.text_field :name %>
    </div>
    <div class="field">
        <%= f.label :redirect_link %><br>
        <%= f.text_field :redirect_link %>
    </div>
    <div class="field">
        <%= f.label :slogan %><br>
        <%= f.text_field :slogan %>
    </div>
    <div class="field">
        <%= f.label :description %><br>
        <%= f.text_field :description %>
    </div>
    <div class="field">
        <%= f.label :social_phrase %><br>
        <%= f.text_field :social_phrase %>
    </div>

    <div class="actions">
    <%= f.submit %>
    </div>
<% end %>
